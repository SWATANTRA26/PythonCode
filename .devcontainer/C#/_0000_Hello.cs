using System;
    Console.WriteLine("Hello, Welcome to C#!");//Used to print.
    //Take Input from console and store in an variable.
    int _input = Console.Read("Enter a number: ");
    Console.Write(_input);
    string input = Console.ReadLine(); //Convert the input string to an integer.
    Console.WriteLine($"You entered: {input}");